# Domain modules

The application is a modular monolith. Each directory below is a domain
boundary; implementation layers can be added within a domain when needed.

The module list follows the architecture baseline and keeps presentation,
application rules, and infrastructure concerns from being coupled together.
