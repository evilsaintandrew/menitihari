import { PasswordResetScreen } from "@/components/auth/password-reset-screen";

export const metadata = {
  title: "Reset password | Menitihari",
};

export default function ResetPasswordPage() {
  return <PasswordResetScreen />;
}
